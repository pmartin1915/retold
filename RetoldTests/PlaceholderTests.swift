import XCTest
@testable import Retold

/// Week-0 scaffold placeholder, proving the test target builds and links against the app
/// (TEST_HOST/BUNDLE_LOADER) before any real code exists. Replaced by week-1 tests
/// (docs/PLAN.md section 12) once the recorder, transcript pipeline, and data model land.
final class PlaceholderTests: XCTestCase {
    func testScaffoldBuilds() {
        XCTAssertTrue(true)
    }
}
