# R1 diff audit — Sol, 2026-09-22

Reviewer: Sol (GPT-5 via `pal clink`, `codex`, codereviewer). Input: `git diff 4187d1f..r1-impl --
Retold RetoldTests` and the R1 spec; PLAN readable in the repo. Pre-compile: CI was blocked on
GitHub billing, so no Swift had been compiled. Eight findings.

| # | Finding | Verdict | Action |
|---|---|---|---|
| 1 | Critical: `Episode(title:)` accepted `.proposedByModel`; `confirm()` then made a confirmed model-authored title, and the round-trip fixture persisted one. `proposedQuote(value:span:)` also allows `value != span.text` | **accept** | `Episode` now has only `init(titleQuote:)` (value is `span.text`, via new `Confirmable<String>.proposedQuote(span:)`) and `init(typedTitle:)`; `title` is `private(set)` with `confirmTitle`/`rejectTitle`/`retitle(byUser:)`. Fixtures moved to quotes. New test `testEpisodeTitleIsQuoteOrTyped`. The generic `proposedQuote(_:span:)` stays for non-String values |
| 2 | High: `@MainActor` test class overriding synchronous `setUp`/`tearDown` won't compile in Swift 6 | **accept** | Overrides removed; per-test directory plus `addTeardownBlock` |
| 3 | High: `[Person, Person, Episode, Episode]` literal infers `[Any]`; `insert` won't take it | **accept** | One insert per object |
| 4 | High: `VerifiedSpan` is freely constructible and `setExcerpt` takes arbitrary segments, so model text could be dressed as verified | **defer to R2** | The R2 verifier owns span construction; the R2 spec must make `VerifiedSpan`'s initializer reachable only through the verifier and derive excerpts from a completed `Capture`. `ai/IDEAS.md` |
| 5 | High: `Question` text/template/slots/origin mutable after init; a fixture used two slots from different segments | **partial** | `text`, `templateID`, `slots`, `cue`, `origin` now `private(set)`. The audited assembler and the same-segment rule are R3/R4 |
| 6 | High: `Person`/`Place` names and the period/place/people relationships are writable without confirmation | **defer** | These are user-owned and written by the confirm flow (R7). The structural fix — the model pipeline in a module that can't see persistence constructors — goes to `ai/IDEAS.md` for the R7 spec |
| 7 | INSUFFICIENT_CONTEXT: SwiftData's composite storage may not call `Confirmable.init(from:)`, so a malformed stored value would reach the getter's precondition | **defer, noted** | Only `Confirmable`'s own encoder writes the store, and it only encodes valid states; the spike run will show whether reopen round-trips. `ai/IDEAS.md` |
| 8 | Medium: `RetoldSchemaV1` lists live top-level models, so V1 isn't frozen | **defer** | Standard practice: freeze V1 by copying its models into the `RetoldSchemaV1` namespace when V2 is introduced. Noted in `ai/IDEAS.md` |
